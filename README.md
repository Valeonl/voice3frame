# Voice3Frame 🎙️ — программный комплекс для осуществления голосового управления процессом видеомонтажа

[![Перейти к сайту с описанием системы и проведённого исследования](https://img.shields.io/badge/GitHub_Pages-Voice3Frame_Детальное_Описание-blue?style=for-the-badge&logo=github)](https://valeonl.github.io/voice3frame/)

**Voice3Frame** представляет собой программный комплекс, разработанный для обеспечения голосового управления процессом видеомонтажа в Adobe Premiere Pro и в любом другом совместимом с API видеоредактором.
Система нацелена на повышение эффективности и доступности видеопроизводства, предоставляя интуитивно понятный интерфейс для выполнения/редактирования и исполнения команд.

---

## 🎯 Назначение и Целевая Аудитория

Система Voice3Frame разработана для следующих категорий пользователей и целей:

*   🎓 **Начинающие пользователи Adobe Premiere Pro:** Упрощение освоения сложного интерфейса видеоредактора и выполнение базовых операций с помощью естественного языка.
*   ⏱️ **Оптимизация рутинных операций:** Предоставление опытным пользователям инструмента для ускорения часто повторяющихся действий.
*   ♿ **Повышение доступности:** Альтернативный метод ввода для пользователей, испытывающих затруднения при работе с традиционными устройствами (клавиатура, мышь).
*   🔬 **Исследовательская платформа:** Демонстрация интеграции технологий распознавания речи, обработки естественного языка (с применением LLM) и управления десктопными приложениями.

---

## 🏛️ Архитектура и Компоненты Системы

Voice3Frame имеет модульную архитектуру, обеспечивающую гибкость и возможность независимой разработки и модификации компонентов.

### 🧩 Ключевые Компоненты:

1.  **`Adobe Voice` (Клиентское приложение)**
    *   **Функционал:** Основной интерфейс пользователя для захвата голосовых команд. Осуществляет первичное преобразование речи в текст посредством Web Speech API браузера и передает текстовые данные на серверный компонент.
    *   **Расположение в репозитории:** `adobe_voice/`
    *   **Интеграция с Premiere Pro:** Включает модуль `premiere_controller.py`, который напрямую взаимодействует с Adobe Premiere Pro для выполнения команд.

2.  **`VoiceCommandAPI Backend` (Сервер Интерпретации)**
    *   **Функционал:** "Интеллектуальный центр" системы. Принимает текстовые команды от `Adobe Voice`, анализирует их с помощью Большой Языковой Модели (LLM) и преобразует в структурированный JSON-формат, понятный для исполнительного модуля в `Adobe Voice`.
    *   **Расположение в репозитории:** `voice_command_api/voice_command_api_backend/`
    *   **Конфигурация:** Определения команд, их параметры и примеры для LLM хранятся в файле `prompts/config.json`.

3.  **`VoiceCommandAPI Frontend` (Веб-интерфейс Настройки API)**
    *   **Функционал:** Пользовательский веб-интерфейс, предназначенный для управления конфигурацией `VoiceCommandAPI Backend`. Позволяет редактировать список доступных команд, их описания и примеры формулировок, что улучшает точность интерпретации LLM.
    *   **Расположение в репозитории:** `voice_command_api/voice_command_api_frontend/`

4.  **`github_page` (Сайт Проекта)**
    *   **Функционал:** Статический веб-сайт, размещенный на GitHub Pages, предоставляющий детальное описание проведенного исследования, архитектуры системы, примеров использования и полученных результатов.
    *   **Расположение в репозитории:** `github_page/`

## 📁 Структура Репозитория
Репозиторий организован следующим образом для разделения компонентов и их зависимостей:
```
valeonl-voice3frame/
├── README.md                      # Данный файл описания
├── adobe_voice/                   # Клиентское приложение Adobe Voice (Eel + Python)
│   ├── main.py                    # Точка входа для запуска клиента Adobe Voice
│   ├── premiere_controller.py     # Модуль для взаимодействия с Adobe Premiere Pro
│   ├── requirements.txt           # Python зависимости для Adobe Voice
│   └── web/                       # Файлы веб-интерфейса клиента (HTML, CSS, JavaScript)
│       ├── index.html
│       └── ... (директории scripts, styles)
├── github_page/                   # Исходные файлы для веб-сайта проекта на GitHub Pages
│   └── ...
├── voice_command_api/             # Сервисы для обработки и интерпретации команд
│   ├── voice_command_api_backend/ # Серверная часть API (Flask + Python)
│   │   ├── app.py                 # Точка входа для Flask-приложения (API)
│   │   ├── requirements.txt       # Python зависимости для VoiceCommandAPI Backend
│   │   └── prompts/               # Файлы конфигурации команд и системных промптов для LLM
│   │       ├── config.json        # Основной конфигурационный файл команд (редактируется)
│   │       └── ... (base.md, custom.md - базовые/пользовательские текстовые промпты)
│   └── voice_command_api_frontend/ # Клиентская часть для настройки VoiceCommandAPI (React)
│       ├── package.json           # Зависимости Node.js и скрипты для VoiceCommandAPI Frontend
│       └── src/                   # Исходный код React-приложения
│           └── ...
└── .github/                       # Конфигурации для GitHub Actions (например, для деплоя GitHub Pages)
    └── workflows/
        └── pages.yml
```

## 🛠️ Технологии и Системные Требования

Для корректной работы и разработки системы Voice3Frame необходимы следующие компоненты:

### Общие требования:

- Git (для клонирования репозитория).
- Adobe Premiere Pro (рекомендована версия 2023 или новее).

### 1. Компонент adobe_voice (Клиент):

- **Язык**: Python 3.x
- **Основные библиотеки**:
  - Eel: Для создания десктопных приложений с веб-интерфейсом.
  - Requests: Для выполнения HTTP-запросов к VoiceCommandAPI Backend.
  - Pymiere: Для взаимодействия с Adobe Premiere Pro.
- **Окружение**: Современный веб-браузер с поддержкой Web Speech API (например, Google Chrome, используемый Eel по умолчанию).
- **Файл зависимостей**: `adobe_voice/requirements.txt`

### 2. Компонент voice_command_api/voice_command_api_backend (Сервер API):

- **Язык**: Python 3.x
- **Основные библиотеки**:
  - Flask: Микрофреймворк для создания REST API.
  - Requests: Для внутренних HTTP-запросов (если потребуются).
  - python-dotenv: Для управления переменными окружения (например, ключом API).
  - Google Generative AI SDK (google-generativeai): Для взаимодействия с LLM Google Gemini.
- **Файл зависимостей**: `voice_command_api/voice_command_api_backend/requirements.txt`
- 🔑 **Требуется API ключ для Google Gemini** (детали в разделе "Настройка").

### 3. Компонент voice_command_api/voice_command_api_frontend (Фронтенд настройки API):

- **Среда**: Node.js и менеджер пакетов npm или yarn.
- **Основные библиотеки**:
  - React: Библиотека для создания пользовательских интерфейсов.
  - TypeScript: Для статической типизации.
  - Axios: Для выполнения HTTP-запросов к VoiceCommandAPI Backend.
- **Файл зависимостей**: `voice_command_api/voice_command_api_frontend/package.json`

## 🚀 Инструкция по Запуску Компонентов
### ⚙️ Шаг 0: Начальная Подготовка
1. Клонируйте репозиторий:
```
git clone https://github.com/Valeonl/voice3frame.git
cd voice3frame
```
2. Убедитесь в наличии установленных Python 3.x, Node.js (с npm или yarn).
3. Запустите Adobe Premiere Pro и откройте проект, с которым планируется работа.

### 🧠 Шаг 1: Запуск VoiceCommandAPI Backend (Сервер Интерпретации)
Это основной логический компонент, который должен быть запущен для обработки команд.
1. Перейдите в директорию бэкенда:
```
cd voice_command_api/voice_command_api_backend
```
2. Создайте и активируйте виртуальное окружение Python (рекомендуется для изоляции зависимостей):
```
python -m venv venv

# Windows:
# venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate
```
3. Установите Python-зависимости:
```
pip install -r requirements.txt
```
4. Настройте API ключ для Google Gemini:
- Получите ваш персональный API ключ на [Google AI Studio](https://www.google.com/url?sa=E&q=https%3A%2F%2Faistudio.google.com%2Fapp%2Fapikey)
- В директории voice_command_api/voice_command_api_backend/ создайте файл .env.
- Добавьте в него следующую строку, заменив ВАШ_GOOGLE_API_КЛЮЧ на полученный ключ:
```
GOOGLE_API_KEY=ВАШ_GOOGLE_API_КЛЮЧ
```
5. Запустите Flask-сервер:
```
python app.py
```
В консоли должно появиться сообщение о запуске сервера (обычно на ```http://localhost:5000```). 

<b>Не закрывайте данное окно терминала во время работы системы.</b>

## 🎤 Шаг 2: Запуск Adobe Voice (Клиентское Приложение)
Это приложение будет захватывать вашу речь.
1. Откройте новое окно терминала или командной строки.
2. Перейдите в директорию клиентского приложения:
```
cd adobe_voice
```
(Если текущая директория voice_command_api/voice_command_api_backend, выполните cd ../.. затем cd adobe_voice)
3. Создайте и активируйте виртуальное окружение Python (можно использовать отдельное):
```
python -m venv venv
# Windows:
# venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate
```
4. Установите Python-зависимости:
```
pip install -r requirements.txt
```
5. Запустите клиентское приложение Adobe Voice:
```
python main.py
```
После запуска появится компактное окно приложения, готовое к приему голосовых команд.

## 🛠️ Шаг 3 (Опционально): Запуск VoiceCommandAPI Frontend (Веб-интерфейс Настройки Команд)
Данный компонент используется для кастомизации и расширения набора распознаваемых команд.
1. Откройте новое окно терминала или командной строки.
2. Перейдите в директорию фронтенд-приложения для настройки API:
```
cd voice_command_api/voice_command_api_frontend
```
3. Установите Node.js зависимости:
```
npm install
# или, если используется yarn:
# yarn install
```
4. Запустите React-приложение в режиме разработки:
```
npm start
# или:
# yarn start
```
В вашем браузере автоматически откроется страница (обычно http://localhost:3000), где будет доступен интерфейс для редактирования файла config.json.

<b>Поздравляем! Система Voice3Frame готова к использованию. 🎉</b>

# 🗣️ Использование Клиента Adobe Voice

1. Убедитесь, что VoiceCommandAPI Backend запущен, и Adobe Premiere Pro открыт с нужным проектом.
2. В окне Adobe Voice:

   - **Кнопка Микрофона**: Нажмите для начала записи команды. Произнесите команду. Повторное нажатие остановит запись и отправит команду на обработку.

   - **Непрерывный Режим** (иконка <i class="fas fa-infinity"></i> слева вверху):
     - Кликните на иконку для отображения панели настроек непрерывного режима.
     - Активируйте переключатель "Включен".
     - Нажмите кнопку микрофона. Система будет непрерывно анализировать речь. Команда будет отправлена автоматически после обнаружения паузы в речи (длительность паузы настраивается в панели).

   - **Настройки** (иконка <i class="fas fa-cog"></i> или шестеренка справа вверху):
     - Позволяет настроить горячую клавишу для активации/деактивации микрофона (по умолчанию "Space").

   - **Поле Транскрипции**: Отображает распознанный текст. При необходимости текст можно отредактировать вручную и отправить повторно кнопкой "Отправить".

   - **Кнопки "Отменить"/"Повторить"**: Для навигации по истории изменений текста в поле транскрипции.

# ✍️ Расширение Системы: Добавление и Изменение Команд

Система Voice3Frame спроектирована с учетом возможности расширения. Добавление новых команд включает два основных этапа:

## 1. Конфигурирование Команды для LLM (в prompts/config.json)

- Файл `voice_command_api/voice_command_api_backend/prompts/config.json` является основным источником информации для LLM при интерпретации команд.
- Редактирование может осуществляться напрямую или через VoiceCommandAPI Frontend.
- **Структура объекта команды в config.json**:
  - `className` (String): Уникальное имя команды. Используется в `premiere_controller.py` для сопоставления с соответствующей Python-функцией.
  - `description` (String): Краткое описание назначения команды для LLM.
  - `displayName` (String): Отображаемое имя команды (используется в интерфейсе настройки).
  - `examples` (Array of Strings): Примеры различных формулировок данной команды пользователем. Критически важно для обучения LLM (few-shot learning).
  - `id` (String): Уникальный идентификатор для использования во фронтенд-части.
  - `parameters` (Array of Objects): Описание параметров, которые команда может принимать (имя параметра, описание, значение по умолчанию, возможные значения).

## 2. Реализация Логики Выполнения Команды (в adobe_voice/premiere_controller.py)

- После определения новой команды и ее `className` в `config.json`, необходимо реализовать ее фактическое выполнение в Adobe Premiere Pro.
- Откройте файл `adobe_voice/premiere_controller.py`.
- В классе `PremiereController` добавьте новый метод, имя которого **точно соответствует** значению `className` из `config.json`.
- Данный метод должен принимать аргументы, соответствующие параметрам, определенным для команды в `config.json`. Внутри метода используйте библиотеку `pymiere` для взаимодействия с Adobe Premiere Pro.
- Зарегистрируйте вызов нового метода в функции `process_commands` (в `premiere_controller.py`), добавив соответствующий блок `elif command == "your_new_className":`.

Пример добавления команды "Применить эффект 'Черно-белый'":
1. config.json:
```
{
  "className": "apply_bw_effect",
  "description": "Применяет черно-белый эффект к выделенному клипу.",
  "displayName": "Применить Ч/Б эффект",
  "examples": ["сделай клип черно-белым", "наложить эффект чб", "черно-белый фильтр"],
  "id": "cmd_apply_bw",
  "parameters": [] // Для простоты, без параметров
}
```
2. premiere_controller.py (в классе PremiereController):
```
def apply_bw_effect(self) -> Dict[str, Any]:
    if not self.connected:
        return self._emulate_apply_bw_effect() # Метод эмуляции для тестирования
    try:
        # Примерный код для применения эффекта (реальный код может отличаться)
        # sequence = self.project.activeSequence
        # selected_clip = # ... (логика получения выделенного клипа)
        # if selected_clip:
        #     effect = sequence.project.getVideoEffectByName("Black & White") # Имя эффекта может отличаться
        #     if effect:
        #         selected_clip.addVideoEffect(effect)
        #         return {"status": "success", "message": "Ч/Б эффект применен"}
        # return {"status": "error", "message": "Клип не выделен или эффект не найден"}
        logger.info("[ЭМУЛЯЦИЯ] Применен Ч/Б эффект") # Заглушка
        return {"status": "success", "message": "[ЭМУЛЯЦИЯ] Ч/Б эффект применен"}
    except Exception as e:
        logger.error(f"Ошибка при применении Ч/Б эффекта: {str(e)}")
        return {"status": "error", "message": str(e)}
```
3. premiere_controller.py (в функции process_commands):
```
elif command == "apply_bw_effect":
    results.append(premiere.apply_bw_effect())
```

# 🌐 Дополнительная Информация и детали исследования
Для получения исчерпывающей информации о теоретических основах, методологии исследования, детальном анализе архитектуры, полученных результатах и их обсуждении, пожалуйста, обратитесь к веб-сайту проекта:
![image](https://github.com/user-attachments/assets/7b45a6e9-3e44-4557-8815-fa7b11551c94)
[![Перейти к сайту с описанием системы и проведённого исследования](https://img.shields.io/badge/GitHub_Pages-Voice3Frame_Сайт-blue?style=for-the-badge&logo=github)](https://valeonl.github.io/voice3frame/)

# 📜 Лицензия
Данный проект распространяется на правах open-source проекта. Я как его автор буду рад, если он получит развитие и станет полезным Вам при решении ваших профессиональных и творческих задач.

[![Спроси DeepWiki о репозитории](https://deepwiki.com/badge.svg)](https://deepwiki.com/Valeonl/voice3frame)
